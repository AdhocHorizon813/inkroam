---
title: 范数与距离：从二维向量开始（测试笔记）
description: 用二维向量检查课程归档、正文搜索和公式摘要的展示。
date: 2026-09-19
tags: [思考]
readingTime: 2 分钟
aiGenerated: true
draft: true
---
> 本文是 AI 生成的页面测试内容，不是正式课程笔记；确认功能后可删除。

## 范数：测量向量的长度

在二维实向量空间中，欧几里得范数为 $\lVert x\rVert_2=\sqrt{x_1^2+x_2^2}$。测试词“蓝色尺子”用于确认正文搜索可以找到本节，而不依赖文章标题。

## 范数与距离的关系

由范数可以定义距离 $d(x,y)=\lVert x-y\rVert_2$。例如 $x=(3,4)$、$y=(0,0)$，则距离是 $5$。范数测量一个向量，距离比较两个点。

## 范数的三角不等式

欧几里得范数满足 $\lVert x+y\rVert_2\leq\lVert x\rVert_2+\lVert y\rVert_2$。几何上，直接连接两个端点，不会比绕经第三个点更长。

## 范数收敛

序列在范数意义下收敛到 $x$，表示 $\lVert x_n-x\rVert_2\to 0$。检索“范数”应该将这些章节归并为一篇结果，并允许展开其余匹配。

## 范数练习

取 $u=(1,0)$、$v=(0,1)$，计算三个范数 $\lVert u\rVert_2$、$\lVert v\rVert_2$、$\lVert u+v\rVert_2$，再检查三角不等式。答案依次为 $1$、$1$、$\sqrt{2}$。

## PDF 内嵌测试

以下为用户提供的 PDF，用于本地检验页内阅读；与上方 AI 生成的测试文字无关。

| ::pdf-viewer{src="/pdfs/linear-functional-analysis-test.pdf" title="Linear Functional Analysis · PDF 内嵌测试"} |
| :--------------------------------------------------------------------------------------------------------------: |
