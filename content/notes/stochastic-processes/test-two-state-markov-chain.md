---
title: 两状态马尔可夫链：晴天与雨天（测试笔记）
description: 用一个虚构的天气模型检查课程分类、矩阵公式和章节搜索。
date: 2026-09-19
tags: [思考]
readingTime: 2 分钟
aiGenerated: true
draft: true
---
> 本文是 AI 生成的页面测试内容，不是正式课程笔记。以下概率为虚构示例，不用于真实天气预测。

## 状态与一步转移

设状态依次为晴天、雨天。模型假定明天的状态只依赖今天的状态。测试词“雨滴计数器”用于检查正文检索与章节跳转。

## 转移矩阵

矩阵的行表示今天，列表示明天，每行概率之和为 $1$：

$$
P=\begin{pmatrix}0.8 & 0.2\\0.4 & 0.6\end{pmatrix}.
$$

如果今天是晴天，明天仍是晴天的概率为 $0.8$。

## 两步转移

今天为晴天时，两天后为晴天的概率是 $0.8\times0.8+0.2\times0.4=0.72$。这里枚举了中间一天的两种可能状态。

## 平稳分布

采用行向量记法，平稳分布满足 $\pi P=\pi$ 且分量之和为 $1$。本例为 $\pi=(\frac{2}{3},\frac{1}{3})$，可代回转移矩阵验证。
